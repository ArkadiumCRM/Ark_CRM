# Backups (rotating, nicht checked-in)
backups/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*~

# Node (future code phase)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.local
.env.*.local
.env.brain

# Build artifacts (future)
dist/
build/
.next/
out/

# Python (autoresearch prototype)
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/

# Logs
*.log

# Large binaries we reference but don't version
Grundlagen MD/*.xlsx

# Audio/Video in raw (assessments, interviews, recordings — NEVER commit, 100MB+)
raw/**/*.mp4
raw/**/*.wav
raw/**/*.mp3
raw/**/*.m4a
raw/**/*.mov
raw/**/*.webm
raw/**/*.mkv

# Claude Code local state (sessions/cache — not project-config)
.claude/settings.local.json

# Unbenannt-Ordner (offensichtlich Temp)
Unbenannt/
Unbenannt.canvas

# Temporary / staging
*.tmp
*.bak

# Obsidian local metadata
.obsidian/workspace.json
.obsidian/graph.json
.obsidian/workspace-mobile.json

# Playwright MCP session caches
.playwright-mcp/

# Playwright test artefacts
playwright-report/
test-results/

# Codex-safe worktrees (transient)
.git/codex-worktrees/

# Superpowers plugin local state
.superpowers/

# Claude Code local state (beyond settings.local.json)
.claude/todos/
.claude/plans/
.claude/worktrees/

# HR raw sources (local-only · too large for git · contains videos/InDesign/GoPro)
raw/HR/

# E-Learning raw sources (local-only · ~32GB Videos/Schulungsunterlagen)
raw/E-Learning/

# Claude Preview launch config (local-only)
.claude/launch.json

# Tmp scratch
tmp/

# Brain-CLI metadata
BRAIN.md

# Wiki-chat sub-project (separate Next.js scaffold, eigenes Repo TBD)
wiki-chat/

# graphify output (regenerable artifacts)
graphify-out/cache/
graphify-out/*.html
graphify-out/merged-graph.json
